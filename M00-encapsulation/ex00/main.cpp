#include <iostream>

#include "Bank.hpp"

int main() {
    // Account accountA = Account();
    // accountA._id     = 0;
    // accountA._value  = 100;

    // Account accountB = Account();
    // accountB._id     = 1;
    // accountB._value  = 100;

    // Bank bank       = Bank();
    // bank._liquidity = 999;
    // bank._accounts.push_back(&accountA);
    // bank._accounts.push_back(&accountB);

    // bank._liquidity -= 200;
    // accountA._value += 400;

    // std::cout << "Account : " << std::endl;
    // std::cout << accountA << std::endl;
    // std::cout << accountB << std::endl;

    // std::cout << " ----- " << std::endl;

    // std::cout << "Bank : " << std::endl;
    // std::cout << bank << std::endl;

    return (0);
}
